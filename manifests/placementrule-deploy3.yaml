apiVersion: apps.open-cluster-management.io/v1
kind: PlacementRule
metadata:
  labels:
    app: pacman-app
  name: pacman-deploy3
  namespace: pacman-app
spec:
  clusterSelector:
    matchLabels:
      vendor: EKS
